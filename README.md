This the basic web app which converts the entered text in UPPER CASE or lower case.

This web app is created using ReactJS, Bootstrap.

To use this app first install the node modules by using "npm i".